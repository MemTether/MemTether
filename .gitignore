# 依赖与环境
.venv-*/
venv/
__pycache__/
*.pyc

# 可再生（向量库可由 memory.db 重建）
mem0_store/
archive/
backup/
.backup/

# 运行时
*.db-wal
*.db-shm
*.db-journal
*.log
.sink.lock

# 评测题集的进程间传参文件（2026-09-16）
# ★曾经被当仓库文件提交过一次，结果 hard_holdout 把 CASES 换成 22 题留出集后
#   又被写回，62 题真源被静默污染。现在传参一律走系统临时目录（见 hard_bench.cases_ipc），
#   这里留条规则兜底：任何旧的 _hb_cases.json 都不再进版本库。
_hb_cases*.json

# 轨道A基准数据集（体积大，可重新下载）
bench_data/

# 本地模型（共约 2.4GB）
# ★2026-09-15：这些是**二进制大文件**，早先误提交过一次，把 .git 撑到 1.0GB。
#   现已全部取消跟踪（`git rm --cached`，磁盘文件保留）。
#   历史提交里仍有旧副本，未改写历史（rewrite 风险大）——
#   但从此不再增长。
# 换机器时按下列来源从 hf-mirror 重新下载（hf.co 直连不通，实测 000）：
#   Xenova/bge-m3            -> models/bge_m3/         int8 543MB / fp16 1081MB   (embedding 主力)
#   Xenova/bge-small-zh-v1.5 -> models/bge_small_zh/   fp16 45MB                  (对比用)
#   BAAI/bge-reranker-base   -> models/bge_reranker/   1.11GB                     (cross-encoder 精排)
#   另见 rerank.py 头部注释里 cross_encoder 的导出方式。
models/

# ===== 开源脱敏：私人数据与渲染镜像（2026-09-16）=====
# 本地敏感词表（真名 / 班级 / 安全事件词）—— 只在本机存在，绝不入库。
# ★为什么必须忽略：scan_leaks.py 与 memtether_sync.py 都读它。
#   它一旦进仓库，外置就等于没外置 —— 又回到「用泄密清单去泄密」。
scripts/leak_terms.local.json
# 唯一真源（含真名/学号/本机路径/安全事件）
memory.db
sink.json
# 渲染镜像（内容与 memory.db 等价）
experience.md
facts.md
decisions.md
incidents.md
active_tasks.md
DIGEST.md
HUB_INDEX.md
INDEX.md
profile.md
profile/
astra_verdict_closure.md
astra_dialogue.json
astra_turn_ch.txt
migration_report.json
health_report.json
toolbox.json
# 投影与私有文档
projections/
docs/

# 打包产物（构建配置在仓库根 pyproject.toml；产物不入库）
# ★2026-09-16：原先写的是 `packaging/dist/` —— 那时构建配置错放在 packaging/ 里。
#   现构建配置已搬到仓库根，规则同步搬到根级。
build/
dist/
*.egg-info/

# ===== 二次脱敏：工作区散落的私人笔记与运行时产物（2026-09-16）=====
# ★教训一：上一版**逐个列文件名**，必然漏——
#   写了 astra_turn_ch.txt，实际文件叫 astra_turn_ch**2**.txt；
#   忽略了 *.db-wal/shm，却漏了 board.db 本身。→ 一律用**模式**，不用名字。
# ★教训二：这些文件当初是被 `git add -A` 顺手带进来的。凡"本机跑出来的东西"
#   （结果 json、状态 json、进度 txt、裸 .db）默认都是运行时产物，一律不入库。
# ★本文件属于「发布侧自有」，**不会被 memtether_sync.py 覆盖**（见 RELEASE_OWNED）。

# 私人笔记与操作记录
豆包数据转移记录.md
C盘占用诊断报告.md
astra_turn_ch*.txt
as_talk*.txt
inbox/

# 运行时产物 / 状态文件
run_*.json
*_result.json
*_result.*.json
scorecard.json
watermarks.json
skill_forge_state.json
robocopy*_progress.txt
*.db

# 开发残留
# ★注意是 `_[!_]*` 而不是 `_*`：`__init__.py` 这类 dunder 文件必须保留。
#   （曾经用 `_*.py`，结果 filter-branch 把 packaging/src/memtether/__init__.py 一起删了）
_[!_]*.py
# ★用 `*.bak*` 而不是 `*.bak` + `*.bak_*`：快照名形如 `<file>.bak-<YYYYMMDD-HHMMSS>`（连字符），
#   旧的两条规则都匹配不到 —— 实测 9 个快照漏网、其中含内部代号与本机路径，会被 `git add -A` 一并发布。
*.bak*
cleanup_tmp.py

# 生成物一律不入库（2026-09-16）
# 演示库由 scripts/make_demo_db.py 用固定随机种子产出，逐字节可复现；
# 向量索引（demo/mem0_store）也在这里面。理由：生成物入库必然与生成脚本**漂移**
#   —— 改一句模板、库没重新生成，别人拿到的就是旧内容。
# 跑一次 `python scripts/make_demo_db.py` 即可（自带三项自检）。
demo/
# 拒答判据的停用二元组缓存（refuse_live.py 生成，含本机语料用词分布，属"数据"）
_refuse_stop.json

# hubguard 并发治理的运行时文件（2026-09-17）
# ★必须单独列：锁叫 `memory.db.hub.lock`，**不匹配**上面的 `*.db`
*.hub.lock
*.hub.lock.holder
*.journal.jsonl

# 发布归档包（二进制，不进 git 历史；2026-09-17 补）
_archive/
dist/
*.pre-hubguard

# ===== 跨端实验件归档（2026-09-19）=====
# ★归口判据：ACP 直连 / 跨端探针 / 跨端留言，都是「本机两版共存才有意义」的调试与验收件。
#   · ACP 直连是**私有通道**（实测同步等待会长时间占用对方会话，一次 22m46s），不进开源包；
#   · 跨端探针的报告含本机绝对路径（C:\Users\...），更不能发布；
#   · 这些文件的**发布物**是技能 windows-app-data-interconnect/scripts/ 下的同名副本，
#     仓库内只留开发件 ⇒ 整目录归档到 dev/ 并忽略，避免"同一脚本两处漂移"。
# ★为什么整目录忽略而不是逐个列名：逐个列名必漏（本文件 2026-09-16 已有同类教训）。
dev/
